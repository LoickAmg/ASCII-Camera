"""Constantes par défaut."""

# Rampe de caractères du plus dense (pixel sombre) au plus clair (pixel clair).
ASCII_CHARS = "@%#*+=-:. "

DEFAULT_WIDTH = 100

# Un caractère de terminal est environ deux fois plus haut que large : on réduit
# donc le nombre de lignes pour ne pas obtenir une image ASCII étirée verticalement.
DEFAULT_CHAR_ASPECT = 0.55

DEFAULT_FPS = 24.0
